<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Posts</title>
</head>
<body>
    <nav>
        <!-- Navigation goes here -->
    </nav>
    <main>
        <section>
            <form action="/posts/search" method="get">
                <input type="search" name="search" id="search" placeholder="Search">
                <button type="submit">Search</button>
            </form>
        </section>
        <hr>
        <section>
            <% if (Posts) { %>
                <% for (i = 0; i < Posts.length; ++i) { %>
                    <div class="post" onclick="location.assign('/posts/post/<%= Posts[i]._id %>')">
                        <h2 style="margin-bottom: 0px;"><%= Posts[i].Title %></h2>
                        <a href="/profile/<%= Posts[i].Author %>" style="font-size: small; font-weight: lighter; color: darkgrey; margin: 0px;">@<%= Posts[i].Author %></a>
                        <h5 style="font-size: small; font-weight: lighter; color: darkgrey; margin: 0px;">Created: <%= new Date(Posts[i].Created) %></h5>
                        <% if (Posts[i].Updated) { %>
                            <h5 style="font-size: small; font-weight: lighter; color: darkgrey; margin: 0px;">Updated: <%= new Date(Posts[i].Updated) %></h5>
                        <% } %>
                        <p><%= Posts[i].Description %></p>
                    </div>
                <% } %>
            <% } %>
        </section>
    </main>
</body>
</html>